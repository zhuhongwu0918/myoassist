# Reflex Control 反射控制_算法深度解析

> 基于 Song & Geyer (2015) 论文的脊髓反射回路控制器，MyoAssist 实现分析

---

## 一、核心思想：不规划轨迹，只做反射

传统步行控制器的思路是：**规划轨迹 → 跟踪轨迹**。反射控制器的思路完全不同：

> **人类行走不需要大脑实时计算每一步的轨迹。脊髓的局部反射回路，仅靠传感信号的即时响应，就能产生稳定、多样的步态。**

所以这套控制器的输入是**当前时刻的传感信号**，输出是**当前时刻的肌肉激活量**，中间没有任何轨迹规划、预测或优化。逻辑上类似于一系列"if-then"反射规则的叠加。

---

## 二、关键变量：α 角（腿角）

整个控制器围绕一个核心变量运转：

```
α = φ_hip - 0.5 × φ_knee
```

**物理意义**：α 描述整条腿相对于躯干的"后摆/前摆"程度，比单独的髋角更能代表腿的整体姿态。

**目标腿角 α_tgt** 由上层（等效脊髓上）根据步速动态计算：

```python
# reflex_ctrl.py:404
alpha_tgt = α_0 - C_d × D_L - C_v × V_L - θ

# D_L = 骨盆x位置 - 对侧脚踝x位置（前后距离差）
# V_L = 对应速度差
# C_d, C_v = 距离/速度反馈增益（CMA-ES优化参数）
# θ = 当前躯干俯仰角（转换到体坐标系）
```

这个公式的含义：步速越快，骨盆和脚踝的相对速度越大，α_tgt 就越小（腿需要摆得更靠前），实现速度自适应。

---

## 三、步态相位状态机

控制器用一组布尔标志位描述步态子相，由传感信号自主触发切换：

```
     脚触地                              脚离地
       │                                    │
       ▼                                    ▼
  ph_st（支撑相）──────────────────────► ph_sw（摆动相）
   ├─ ph_st_csw  单支撑，对侧在摆动
   ├─ ph_st_st   正常支撑，持续撑地
   └─ ph_st_sw0  准备离地转换（双支撑末期）

  ph_sw 内部 4 个时序子相：
   ├─ sw_1  膝关节屈曲期   ── φ_knee 到达 knee_sw_tgt → 进入 sw_2
   ├─ sw_2  前摆保持期     ── α 减小到 α_tgt → 退出 sw_2
   ├─ sw_3  末期减速制动   ── α 越过 α_tgt+α_delta → HAM/GAS 制动
   └─ sw_4  落脚前精修     ── dα 变正（腿停止前摆）→ 关节角微调
```

---

## 四、10 个反射模块逐一解析

每个模块对应一种生理真实的脊髓反射回路，输出叠加为最终肌肉激活。

### Module 1：支撑相负荷反射（撑地）

**生理原型**：Golgi 腱器官正反馈——脚踩越重，伸肌越用力。

```python
S_GLU_1 = ph_st_st × max(GLU_FG × F_GLU, 0)   # 臀大肌：力反馈驱动
S_VAS_1 = ph_st_st × max(VAS_FG × F_VAS, 0)   # 股四头肌：力反馈驱动
S_SOL_1 = ph_st    × max(SOL_FG × F_SOL, 0)   # 比目鱼肌：力反馈驱动

# ph_st_sw0（离地准备）时：用对侧负荷衰减（重心转移）
# 系数：clip(1 - Tr_St_sup × load_contra, 0, 1)
```

### Module 2：支撑相膝关节稳定（防膝折叠）

```python
S_HAM_2 = ph_st × max(HAM_FG × F_HAM, 0)              # 腘绳肌力反馈
S_VAS_2 = -ph_st × max(VAS_PG × (φ_knee - knee_off), 0) # 膝角超限 → 抑制股四头肌
S_BFSH_2 = ph_st × BFSH_PG × max(φ_knee - knee_off, 0) # 股二头肌短头屈膝制动
```

### Module 3：躯干姿态反射（核心平衡机制）

**躯干前倾（θ > θ_tgt）→ GLU 把髋推后；躯干后倾 → HFL 把髋拉前。**

```python
S_HFL_3 = ph_st × load_ipsi × max(-(HFL_Th×(θ-θ_tgt)) - (HFL_dTh×dθ), 0)
S_GLU_3 = ph_st × load_ipsi × max( (GLU_Th×(θ-θ_tgt)) + (GLU_dTh×dθ), 0)
S_HAM_3 = max(HAM_GLU_SG × S_GLU_3, 0)  # HAM 协同 GLU

# load_ipsi 作为门控：只有脚踩地时躯干反射才生效
```

### Module 4：对侧肢体协调（传出拷贝）

**最有神经科学意义的模块**：本侧支撑腿接收对侧摆动腿的传出拷贝信号，驱动自身的髋关节提前准备。

```python
# c_afferent = 对侧腿在 M6/M8/M10 阶段的肌肉激活输出拷贝
S_HFL_4 = c_ph_sw × (HFL_C_GLU_PG × c_aff_GLU + HFL_C_HAM_PG × c_aff_HAM)
S_GLU_4 = c_ph_sw × (GLU_C_HFL_PG × c_aff_HFL + GLU_C_RF_PG  × c_aff_RF)

# 代码中先算摆动腿再算支撑腿，确保信号可用
# reflex_ctrl.py:438-451
```

### Module 5：踝关节背屈控制（全相位）

```python
S_TA_5    = max(TA_PG × (φ_ankle - ankle_tgt), 0)    # 角度偏差 → 胫前肌背屈
S_TA_5_st = -ph_st × TA_SOL_FG × max(F_SOL, 0)       # 支撑相：SOL力抑制TA（踩地不背屈）
```

### Module 6：摆动相髋关节驱动（α 角误差控制）

```python
S_HFL_6 = ph_sw × max( HFL_PG×(α-α_tgt) + HFL_VG×dα, 0)  # α不够 → HFL 前摆
S_GLU_6 = ph_sw × max(-GLU_PG×(α-α_tgt) - GLU_VG×dα, 0)  # α过了 → GLU 后拉
```

### Module 7：摆动初期屈膝抬脚（sw_1）

```python
S_BFSH_7 = ph_sw_sw_1 × max(-BFSH_VG × dα, 0)  # dα < 0（腿后摆时）→ BFSH 屈膝
```

### Module 8：摆动中期膝关节阻尼（sw_2）

```python
S_RF_8   = ph_sw_sw_2 × max(-RF_VG × dφ_knee, 0)             # 阻止膝过屈
S_BFSH_8 = ph_sw_sw_2 × max(BFSH_VG × dφ_knee, 0)           # 提供屈曲阻尼
           × max(BFSH_PG × (α - α_tgt), 0)
```

### Module 9：摆动末期制动（sw_3，防落地冲击）

```python
S_HAM_9  = ph_sw_sw_3 × max(-HAM_PG × (α - (α_tgt + α_delta)), 0)
S_BFSH_9 = ph_sw_sw_3 × max(BFSH_HAM_SG × (S_HAM_9 - BFSH_HAM_Thr), 0)
S_GAS_9  = ph_sw_sw_3 × max(GAS_HAM_SG  × (S_HAM_9 - GAS_HAM_Thr), 0)

# α_delta：HAM 比 HFL 更早激活的"提前量"，让腿提前减速
```

### Module 10：落脚前关节角精修（sw_4）

```python
S_HFL_10 = ph_sw_sw_4 × max( HFL_PG × (φ_hip - hip_tgt), 0)
S_GLU_10 = ph_sw_sw_4 × max(-GLU_PG × (φ_hip - hip_tgt), 0)
S_VAS_10 = ph_sw_sw_4 × max(-VAS_PG × (φ_knee - knee_tgt), 0)
```

### 最终叠加

```python
stim['GLU']  = 0.01 + M1 + M3 + M4 + M6 + M10
stim['VAS']  = 0.01 + M1 + M2 + M10
stim['SOL']  = 0.01 + M1
stim['HAM']  = 0.01 + M2 + M3 + M4 + M9
stim['GAS']  = 0.01 + M2 + M9
stim['BFSH'] = 0.01 + M2 + M7 + M8 + M9
stim['HFL']  = 0.01 + M3 + M4 + M6 + M10
stim['TA']   = 0.01 + M5
stim['RF']   = 0.01 + M8
# 0.01 = 最低预激活（对应肌梭持续低频放电）
# 最终 clip 到 [0.01, 1.0]
```

---

## 五、神经传导延迟

代码用 `deque` 环形缓冲区模拟神经传导时间，仿真步长 1ms（`delayed=True` 时）：

```python
# reflex_ctrl.py:206-282
LongLoopDelay = 0.015s  # 脊髓上回路额外延迟（大脑参与）
LongDelay     = 0.010s  # 踝关节肌肉（距脊髓最远）
MidDelay      = 0.005s  # 膝关节肌肉
ShortDelay    = 0.003s  # 髋关节肌肉（距脊髓最近）
MinDelay      = 0.001s  # 脊髓内神经元间

# 感觉延迟（传入）：感受器 → 脊髓
# 脊髓上延迟（传入+传出）：感受器 → 大脑 → 脊髓 = 感觉延迟 + LongLoopDelay
# 输出延迟（传出）：脊髓运动神经元 → 肌肉
```

---

## 六、控制器能真正走起来吗？超参数难调吗？

### Q1：这个控制器真的能走起来吗？

**能，而且已经在论文和多个仿真平台上验证过。**

Song & Geyer 2015 原始论文就用这套控制器实现了 2D 肌骨模型的平地行走、上下坡、跑步等多种步态，速度覆盖 0.5~3 m/s。MyoAssist 将其移植到 MuJoCo 的三维肌骨模型上，并在 22/26/80 肌肉模型上均能实现稳定行走。

有几个关键设计保证了鲁棒性：

- **反射正反馈本质上是稳定的**：脚踩越重、伸肌越用力，形成自增益机制，不需要外部稳定器
- **α 角误差驱动摆动**：而非绝对角度目标，对初始姿态误差不敏感
- **相位状态机靠传感事件触发**：自然适应步伐节奏，不依赖固定频率
- **神经传导延迟已内建**：在延迟条件下标定的增益具有真实的鲁棒裕度

实际上在 MyoAssist 中，预优化参数文件（`ctrl_optim/results/preoptimized/`）直接可用于让模型走起来。

### Q2：那么多参数，是不是很难调？

这是这套控制器最大的工程挑战。先看参数规模：

| 模型      | 反射增益 | 初始姿态 | 肌肉缩放 | 外骨骼 | **合计**   |
| --------- | -------- | -------- | -------- | ------ | ---------------- |
| 22肌肉 2D | 51       | 26       | 0        | 0~8    | **77~85**  |
| 26肌肉 3D | 63       | 34       | 0        | 0~8    | **97~105** |

但实际上"难调"是相对的：

**有利因素：**

1. **参数有明确生理意义**，每个增益对应一个可理解的反射，调错了行为是可预测的（比如 `GLU_FG` 调太大 → 模型像木桩一样硬站着）
2. **CMA-ES 不需要梯度**，不需要手动推导，只要能跑仿真就能优化。代码中用 `EvalParallel2` 多进程并行，每代同时评估几十组参数
3. **分层初始化降低了搜索难度**：

   - 先不带外骨骼优化人体反射参数（77个）
   - 固定人体参数，再优化外骨骼样条参数（4~8个）
   - Bootstrapping 机制：先用4点样条优化，再升至N点（继承已有结果）
4. **参数有物理约束**：大多数增益下界为0（不能负反馈），部分有解剖学上界，大幅压缩搜索空间

**不利因素：**

1. **MuJoCo 仿真本身有噪声**，同一参数集在不同随机种子下代价���有���动
2. **代价函数是非凸多峰的**，CMA-ES 容易陷入局部最优（比如只能慢走、不能快走）
3. **仿真速度是瓶颈**：一次完整20秒仿真（`sim_time=20`）在普通CPU上需要几秒，种群规模×迭代次数的总仿真量巨大，需要多核并行
4. **跨模型参数不可直接迁移**：22肌肉模型优化好的参数，用在26肌肉模型上不一定还能走（肌肉结构变了）

**MyoAssist 的实用策略（来自代码）：**

```python
# train.py — 从已有参数热启动
if input_args.param_path is not None:
    loaded_params = np.loadtxt(...)   # 加载之前优化好的 _BestLast.txt
    params_0 = loaded_params          # 以此为起点继续优化，而非从零开始

# sigma_mult：给每个参数设置不同的搜索步长
sigma_mult = np.ones(param_num) * input_args.sigma_gain
# CMA_active = True：主动协方差矩阵更新，收敛更快
```

**结论**：单靠手调几乎不可能，但 CMA-ES + 预优化参数热启动 + 分阶段策略，实际上 1000~3000 代（根据硬件几小时到几天）就能得到可用的步行参数。

---

## 七、部署到真实硬件的传感器需求

### 7.1 外骨骼场景（如 Dephy、HMEDI）

外骨骼是穿在人身上辅助行走的，人体本身有完整的神经肌肉系统，控制器只需提供**辅助扭矩**，不需要完全接管运动。

**最小传感器配置（仅用外骨骼样条控制器）：**

| 传感器             | 安装位置   | 用途                      | 采样率要求 |
| ------------------ | ---------- | ------------------------- | ---------- |
| 足底压力/力传感器  | 鞋垫或踝部 | vGRF → FSM 触地/离地检测 | ≥ 100 Hz  |
| 踝/膝关节编码器    | 外骨骼关节 | 关节角度 → 样条时间进度  | ≥ 200 Hz  |
| 扭矩传感器（可选） | 外骨骼关节 | 力控反馈                  | ≥ 500 Hz  |

**用完整反射控制器的传感器配置（需要模拟代码中的所有感觉信号）：**

| 信号名                                   | 对应传感器                           | 说明                                   |
| ---------------------------------------- | ------------------------------------ | -------------------------------------- |
| `F_GLU`, `F_VAS`, `F_SOL`...       | **肌电传感器 (sEMG)**          | 表面肌电贴片，测肌肉激活 → 代理肌肉力 |
| `phi_hip`, `phi_knee`, `phi_ankle` | **关节编码器** 或 IMU          | 每个关节需要角度信号                   |
| `dphi_hip`, `dphi_knee`              | 编码器微分 或 陀螺仪                 | 关节角速度                             |
| `load_ipsi`                            | **足底压力传感器**             | 同侧肢体负荷（归一化GRF）              |
| `contact_ipsi/contra`                  | 足底接触开关                         | 触地/离地事件                          |
| `theta` (躯干俯仰)                     | IMU（躯干部位）                      | 躯干角度                               |
| `dtheta`                               | IMU 角速度                           | 躯干角速度                             |
| `pelvis_pos`, `pelvis_vel`           | GNSS / 光学动作捕捉 / 腿部运动学积分 | 骨盆位置和速度，用于计算 α_tgt        |

**实际部的挑战**：

- sEMG 信号噪声大、电极移位会漂移，`F_GLU` 等肌肉力信号很难从 sEMG 直接还原
- `pelvis_vel` 在室外环境获取困难（GPS 精度不够，积分漂移）
- 实时延迟：代码中内置的 3~25ms 神经延迟假设，要求感知-计算-执行全链路延迟 < 5ms

**工程简化方案**：仅用外骨骼的 N 点样条控制器（`NPointSplineController`），只需足底压力 + 关节编码器，大幅降低传感器成本，实际产品（如 Dephy ExoBoot）就是这种方案。

---

### 7.2 人形机器人场景

人形机器人没有人体本身的神经肌肉系统，控制器需要**完全接管全身运动**，挑战更大。

**用反射控制器驱动人形机器人的传感器配置：**

| 信号                             | 传感器                        | 备注                               |
| -------------------------------- | ----------------------------- | ---------------------------------- |
| 关节角度`phi_*`                | 每个关节电机编码器            | 标配，精度高，无问题               |
| 关节角速度`dphi_*`             | 编码器微分                    | 需低通滤波                         |
| 足底地面反力`load_ipsi`        | 6轴力/力矩传感器（足底）      | 每条腿各一个，必需                 |
| 接触检测`contact_*`            | 足底压力传感器 / 力传感器阈值 | 可从力传感器提取                   |
| 躯干姿态`theta`, `dtheta`    | IMU（躯干/腰部）              | 9轴IMU，1kHz                       |
| 骨盆速度`pelvis_vel`           | 腿部运动学正解 + IMU积分      | 关键，决定α_tgt准确性             |
| 对侧脚踝位置`talus_contra_pos` | 关节角正向运动学计算          | 由编码器计算得到，不需要额外传感器 |

**"肌肉力"信号的处理**：

人形机器人没有生物肌肉，代码中的 `F_GLU`、`F_VAS` 等需要**映射到电机力矩**：

```
F_GLU → 髋关节伸展力矩  ÷  肌肉力臂
F_VAS → 膝关节伸展力矩  ÷  肌肉力臂
F_SOL → 踝关节跖屈力矩  ÷  肌肉力臂
```

这需要每个关节有力矩传感器（或用电流估计力矩），以及已知的肌肉力臂参数。

**控制输出的映射**：

```
stim['GLU'] (0.01~1.0)  →  髋关节伸展力矩  = stim × F_max_GLU × 力臂
stim['VAS'] (0.01~1.0)  →  膝关节伸展力矩
stim['SOL'] (0.01~1.0)  →  踝关节跖屈力矩
# ... 每块"肌肉"对应一个关节自由度方向的力矩
```

**推荐的实时控制频率：**

| 层级             | 频率       | 内容                                         |
| ---------------- | ---------- | -------------------------------------------- |
| 物理仿真（参考） | 1000 Hz    | 代码中`env.sim.model.opt.timestep = 0.001` |
| 反射控制器计算   | 200~500 Hz | `delayed=True` 时步长 1ms，否则 10ms       |
| 执行器力矩输出   | ≥ 500 Hz  | 力控带宽                                     |
| 传感器采集       | ≥ 1000 Hz | IMU + 编码器                                 |

**与人形机器人主流方案的对比：**

| 特性         | 反射控制                 | MPC/TO               | RL策略               |
| ------------ | ------------------------ | -------------------- | -------------------- |
| 计算量       | 极低（矩阵乘法级别）     | 极高（QP求解）       | 低（神经网络推断）   |
| 传感器要求   | 中等（需力信号）         | 高（需精确状态估计） | 低（关节角速度即可） |
| 可解释性     | 高（每条规则有生理意义） | 中                   | 低                   |
| 未知地形适应 | 中（靠反射鲁棒性）       | 高（实时优化）       | 高（训练覆盖）       |
| 实时性       | 优秀                     | 受算法耗时限制       | 优秀                 |
| 调参难度     | 中（CMA-ES + 先验）      | 低（模型驱动）       | 高（reward设计）     |

---

## 八、参数分组与物理意义速查

```
[0:5]    躯干和速度控制：θ_tgt, α_0, α_delta, C_d, C_v
[5:7]    相位切换阈值：Tr_St_sup (离地转换), Tr_Sw (摆动触发)
[7:12]   关节目标角：knee_tgt, knee_sw_tgt, knee_off_st, ankle_tgt, mtp_tgt
[12:16]  Module 1 增益：GLU/VAS/SOL/FDL 力反馈（支撑相撑地）
[16:20]  Module 2 增益：HAM/VAS/BFSH/GAS（膝关节稳定）
[20:25]  Module 3 增益：HFL/GLU 的角度+速度阈值，HAM协同（躯干平衡）
[25:30]  Module 4 增益：对侧腿传出拷贝（左右协调）
[30:34]  Module 5 增益：TA/EDL（踝背屈控制）
[34:38]  Module 6 增益：HFL/GLU α角误差驱动（摆动髋）
[38:40]  Module 7 增益：BFSH（摆动初期屈膝）
[40:43]  Module 8 增益：RF/BFSH（摆动中期膝关节阻尼）
[43:48]  Module 9 增益：HAM/BFSH/GAS（摆动末期制动）
[48:51]  Module 10 增益：HFL/GLU/VAS（落脚前精修）

--- 2D 模型到此 51 个参数 ---

[51:63]  3D 扩展：冠状面控制（HAB/HAD 外展内收肌），额外 12 个参数

--- 反射参数结束 ---

[63/51 : 63/51+34/26]  初始姿态参数（关节角度、骨盆速度）
[...  : -spline_n]    肌肉长度缩放
[-spline_n : ]        外骨骼样条参数（4 或 2×N 个）
```

---

## 九、参考文献

1. **Song & Geyer (2015)**. "A neural circuitry that emphasizes spinal feedback generates diverse behaviours of human locomotion." *The Journal of Physiology*, 593(16), 3493-3511.
2. **Geyer & Herr (2010)**. "A muscle-reflex model that encodes principles of legged mechanics produces human walking dynamics and muscle activities." *IEEE TNSRE*.
3. **Poggesnsee & Collins (2021)**. "Effect of ankle exoskeleton stiffness on energy cost of walking with flexible structures." *Science Robotics*. DOI: 10.1126/scirobotics.abf1078
4. **CMA-ES**: Hansen et al. https://github.com/CMA-ES/pycma
